const pensilPrice = 15.678;
const cakePrice = 123.965;
const penPrice = 90.2354;

const maxPrice = Math.max(penPrice, cakePrice, penPrice);

const minPrice = Math.min(penPrice, cakePrice, penPrice)

document.writeln(
    `Max price : ${maxPrice}\nMin price: ${minPrice}`
);

